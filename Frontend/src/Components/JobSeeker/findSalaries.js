import React from "react";
import MainHeader from "./mainHeader";

const FindSalaries = (props) => {
  return (
    <div>
      <MainHeader currentTab="findSalaries"></MainHeader>
      <h1>Find Salaries</h1>
    </div>
  );
};

export default FindSalaries;
